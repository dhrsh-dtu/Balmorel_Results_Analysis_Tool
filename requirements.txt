# Webapp dependencies — read by Streamlit Community Cloud.
# No pybalmorel or gamsapi here: the dashboard does not need GAMS.
# Constraints kept open so Cloud's resolver can pick compatible versions.

streamlit>=1.40
pandas>=2.0
pyarrow>=14
numpy>=1.26
plotly>=5.20
